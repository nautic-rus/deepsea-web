import {Material} from "../classes/material";

export interface Equipment {
 id: string;
 name: string;
 desc: string;
 zone: string;
 zone_desc: string;
 system: string;
 x: number;
 y: number;
 z: number;
 stock_code: string;
}
